name: gather
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.6 <4.0.0'

dependencies:
  file_manager: ^1.0.2
  flutter:
    sdk: flutter
  google_fonts: ^6.1.0
  path: ^1.8.3
  path_provider: ^2.1.1
  http: ^1.0.0
  permission_handler: ^11.0.1
  firebase_core: ^2.22.0
  firebase_storage: ^11.2.6
  flutter_image_compress: ^2.1.0
  image_picker: ^0.8.7+5
  firebase_app_check: ^0.10.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/